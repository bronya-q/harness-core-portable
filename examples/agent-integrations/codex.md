# Codex-style agent context

Use this project as a portable memory/context layer.

```bash
python harness.py demo --offline
python harness.py doctor
python harness.py memory list --scope character:demo
```

For project-level context, keep `AGENTS.md` at repo root.
