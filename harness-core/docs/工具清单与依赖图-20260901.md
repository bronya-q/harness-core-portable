# 工具清单与依赖图（收敛/清理）· 2026-09-01

> 原则：不再加新轮子，收敛为统一 CLI。
> 数据源：`tools_inventory.json` / `tools_dependency.dot` / `tools_legacy.json`

---

## 一、统一入口

```bash
cd ~\.agents\skills\long-term-memory-emotion

python harness.py status
python harness.py audit
python harness.py measure congruence --limit 200
python harness.py plugins status
python harness.py persona validate --name demo-storykeeper
python harness.py gold export --gold recall_gold_independent_v2.json
python harness.py snapshot
python harness.py tools
```

## 二、工具分类

| 领域 | 主要工具 |
|---|---|
| 记忆 | memory_store / vector_worker / recall_context / semantic_search |
| 情感 | nine_dim / emotion_projection / need_projection |
| 人格 | humanization / user_model / mind_evolution / perspective_card |
| 审计测量 | measurement / production_gate / quarterly_audit / harness_selfcheck |
| 插件 | plugin_audit / plugin_sandbox |
| 运行时 | runtime_policy / runtime_preflight / generate_manifest |
| 种子/测试 | seed_* / *_test |

## 三、已标记 legacy/deprecated（15 个）

不再推荐手动使用，但不删除：

```text
daily_report
rating_snapshot
gold_human_label_export
gold_human_label_import
gold_sampler
plugin_runtime_verify
queue_health
memory_health_report
drift_matrix
leakage_matrix
congruence_probe
recall_labeling
blind_review_export
profile_layers
review_queue
```

对应推荐入口：

```text
audit / measure / gold / plugins / snapshot（通过 harness.py）
```

## 四、依赖图

- 机器可读：`tools_dependency.dot`
- 工具清单：`tools_inventory.json`
- legacy 清单：`tools_legacy.json`

## 五、收敛原则

1. 统一从 `harness.py` 进入；
2. 重复工具标记 legacy，不删除；
3. 单一数据源：memory.db / gold 文件 / policy / inventory；
4. 新功能优先并入现有入口，不新建单点脚本。

---

## 六、harness.py 已升级

```text
audit     = 聚合 production_gate + quarterly + health + snapshot
measure   = 细粒度 forward（recall/recall-pool/congruence/anthro/self-reveal/leakage/narrative）
gold      = 细粒度 forward（export/import/sample/human-export/human-import）
plugins   = 细粒度 forward（status/audit/verify/set/run）
```

> 后台实现仍未合并，只是收敛入口；稳定后可考虑真正合并重复后台。
