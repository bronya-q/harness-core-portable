# Harness Core Portable Pack

> 一个可迁移、可自检的心智模型 / 记忆系统 / 长期情感核心包。
> 供其他 agent / 其他电脑导入使用。

## 使用
1. 解压到任意目录；
2. 先读 `PORTABLE_AGENTS.md`；
3. 运行 `python harness.py audit` 做自检；
4. 依赖：Python 3.13 + SQLite；可选 Ollama。

## 不含
- PII / API Key / 真实对话原文 / 大模型文件；
- 原用户绝对路径（已替换为 `~`）。
